# Copie este arquivo para ".env.local" (mesma pasta) e preencha com sua chave real.
# O arquivo ".env.local" NUNCA deve ser commitado — ele já está no .gitignore.

GROQ_API_KEY=sua_chave_aqui

# Opcional — troque o modelo usado (padrão: openai/gpt-oss-120b)
# GROQ_MODEL=openai/gpt-oss-120b
